apiVersion: v1

kind: ConfigMap

metadata:
  name: my-api-config

data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Information"
        }
      },
      "FeatureEnabled": true
    }

  nginx.conf: |
      server {
        listen 8080;
      }