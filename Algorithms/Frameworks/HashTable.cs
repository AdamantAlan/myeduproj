using Algorithms.Patterns.HashTable;

namespace Algorithms.Frameworks
{
    internal class HashTable
    {
        private readonly CountingTechnique _countingTechnique = new();
        private readonly KV_VK _kv_vk = new();
        private readonly ChoiceKey _choiceKey = new();
    }
}
