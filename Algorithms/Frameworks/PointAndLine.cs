using Algorithms.Patterns.PointAndLine;

namespace Algorithms.Frameworks
{
    internal class PointAndLine
    {
        private readonly PointsMethod _pointsMethod = new();
        private readonly TwoPointLines _twoPointsLinesMethod = new();
        private readonly LinesMethod _linesMethod = new();
    }
}